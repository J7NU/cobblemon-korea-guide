# 코블몬 한국어 가이드 — 다음 버전 기능 제안 (C)

> **구현 금지 문서.** 아래는 제안일 뿐이며 HTML에 반영하지 않음.
> 근거는 작업 B에서 가이드를 직접 구동하며 관찰한 마찰점.
> 제약 준수: **단일 HTML · 300KB 이하 · 저사양 기기 구동**(외부 의존성/빌드 없이 순수 HTML+JS로 실현 가능한 안만 제시).

---

## 관찰된 마찰점 (구동 근거)
- 검색은 **DEX(포켓몬)+ITEMS(아이템)만** 대상 → "피카츄 어디서 잡아?"처럼 **바이옴/미션/상성 내용을 검색 불가**.
- 바이옴 탭에서 포켓몬을 봐도 **해당 종의 타입·진화·희귀도로 바로 연결되는 링크가 없음** → 탭을 오가며 수동 대조.
- 상성표는 타입 버튼을 일일이 눌러야 하고 **검색/즐겨찾기 불가**.
- 진행상황(내가 잡은 종) **체크리스트/저장 없음** → 재방문 시 처음부터.
- 탭 상태가 **URL에 없음** → 특정 탭/검색결과 **공유·북마크 불가**.
- 바이옴 목록이 길지만 **희귀도·타입·시간(낮/밤/물속) 필터가 없음**.

---

## 제안 (8개)

### P1. 통합 검색(바이옴·미션·상성 포함)
현재 검색 인덱스에 바이옴 스폰 행·미션·타입명을 추가. "피카츄" 검색 시 **"숲(uncommon)에서 스폰" 결과가 함께** 뜨게.
- 저사양 영향: 인덱스 소폭 증가뿐, 라이브러리 불필요. 낮음.

### P2. 포켓몬 → 상세 크로스링크
바이옴/검색 결과의 포켓몬 이름 클릭 시 **모달**로 타입·진화·주 스폰 바이옴 요약 표시(기존 데이터 재사용).
- 저사양 영향: 단일 모달 DOM 재사용. 낮음.

### P3. 잡은 포켓몬 체크리스트 (localStorage)
각 포켓몬 옆 체크박스 + `localStorage` 저장 + 상단 "n/151 수집" 진행바.
- 저사양 영향: localStorage만 사용, 네트워크 0. 낮음.

### P4. 바이옴 필터 (희귀도·시간·타입)
바이옴 탭 상단에 토글 칩(흔함/보통/희귀/초희귀, 낮/밤/물속). 클릭 시 목록 즉시 필터.
- 저사양 영향: 기존 배열 필터. 낮음.

### P5. URL 해시 라우팅
`#tab=biome`, `#q=피카츄` 형태로 탭/검색 상태를 해시에 반영 → 새로고침·공유·북마크 유지.
- 저사양 영향: `hashchange` 리스너 1개. 매우 낮음.

### P6. 상성 계산기(2타입 동시)
상성표 탭에서 방어 타입 2개 선택 시 **합산 배율(4x/2x/0.25x…)** 계산 표시. 실전 대전에 유용.
- 저사양 영향: 기존 ATK 테이블 재사용, 순수 계산. 낮음.

### P7. 오프라인/PWA 캐싱 힌트
단일 HTML이라 이미 오프라인 친화적. `<meta>` + 간단 안내로 "브라우저에 저장/홈 추가" 유도(서비스워커는 파일 프로토콜 제약상 선택).
- 저사양 영향: 없음(안내 텍스트). 낮음.

### P8. 스폰 조건 배지 표준화
`낮/밤/물속/지하` 등 조건을 색상 배지로 통일하고 범례 1줄 추가 → 조건 가독성↑.
- 저사양 영향: CSS만. 매우 낮음.

---

## Top 3 추천

1. **P1 통합 검색** — 가장 큰 마찰(정보가 있어도 못 찾음)을 직접 해소. 기존 데이터 재사용으로 구현 비용 대비 효용 최고.
2. **P3 수집 체크리스트(localStorage)** — 가이드를 1회성 참고에서 **재방문형 진행 도구**로 승격. 저사양·오프라인에서도 완결.
3. **P2 크로스링크 모달** — 탭 왕복이라는 반복 마찰을 제거, P1/P4와 시너지. 단일 모달로 저비용.

> 세 안 모두 외부 라이브러리·네트워크 없이 순수 HTML+JS+localStorage로 구현 가능 → 단일 HTML·300KB·저사양 제약 충족.
