# 코블몬 한국어 가이드 v2 — QA·데이터검증 리포트

- 대상 파일: `cobblemon-korea-guide.html`
- 검증 기준 버전: **Cobblemon 1.7.3** (최신 1.7 태그, GitLab `cable-mc/cobblemon`)
- 데이터 소스: 원본 스폰 JSON `common/src/main/resources/data/cobblemon/spawn_pool_world/` (824개 파일) + PokeAPI (`/api/v2/pokemon-species/{name}`, `names[].language.name=="ko"`)
- 검증 일자: 2026-07-04
- 파일 크기: 86,351 bytes (< 300KB, 단일 HTML 유지) · 디자인/구조 불변 (데이터 값만 수정)

> **저장소 위치 정정:** 지시서의 `github.com/Cobblemon/Cobblemon`은 현재 404. 코블몬 공식 저장소는 **GitLab `gitlab.com/cable-mc/cobblemon`** 으로 이전됨. 1.7 계열 최신 태그 = `1.7.3`.

---

## 작업 A — 게임 데이터 검증

### A-1. 포켓몬 한국어 이름 (320종) — PokeAPI 대조

- DEX 배열 320개 엔트리 추출 → 319개 고유 영문키 (중복 1건, 아래 B-2 참조).
- 319종 전수를 PokeAPI 한국어 공식명과 대조.
- **결과: 불일치 0건.** 괄호 주석(예: `프리져(데이터팩)`)·성별기호(♀/♂)를 정규화 후 비교했을 때 가이드의 한글명은 **PokeAPI 공식 한국어명과 100% 일치**.
- ⇒ 한국어 이름 관련 **HTML 수정 없음** (수정 불필요).

### A-2. 바이옴 스폰 — 원본 스폰 JSON 대조

- "바이옴 스폰" 탭 16개 바이옴 · 90개 포켓몬 행을 추출.
- 각 행을 `spawn_pool_world` 원본과 대조: (a) 스폰 존재 여부, (b) 바이옴 태그 카테고리 일치, (c) 레어리티 버킷 일치(가이드 `ultra`↔코블몬 `ultra-rare`).
- 전설/화석/데이터팩 소환 4행(프리져·레쿠쟈·릴링(화석)·뮤츠(네더의 별))은 자연 스폰 대상이 아니므로 대조 제외(가이드도 별도 표기).
- **결과: 확인된 오류 3건 → 전부 HTML 반영.**

| # | 위치 | 항목 | 가이드(수정 전) | 원본 데이터 | 근거 파일 | 조치 |
|---|------|------|----------------|-------------|-----------|------|
| A-2-1 | 산·봉우리 | 암멍이 (Rockruff) | uncommon 스폰 | **스폰 정의 없음** (`spawn_pool_world`에 rockruff 스폰 파일/엔트리 부재) | `spawn_pool_world/` 전수 검색 결과 0건 | **행 삭제** (1.7.3에서 야생 자연 스폰 안 됨) |
| A-2-2 | 늪지 아님/네더 | 헬가 (Houndoom) | uncommon(보통) | 네더에서 **common** 버킷 (`"bucket":"common", biomes:["#minecraft:is_nether"]`) | `0229_houndoom.json` | **common으로 정정** |
| A-2-3 | 타이가 | 앱솔 (Absol) | uncommon(보통) | **rare** 버킷 (`"bucket":"rare", biomes:[is_mountain, is_snowy_forest, is_taiga]`) | `0359_absol.json` | **rare로 정정** |

나머지 87행은 원본 스폰 데이터와 정합(바이옴 카테고리·버킷 일치, `is_overworld`/`is_temperate` 광역 태그 포함). 상성표 데이터는 지시대로 검증·수정 대상에서 제외.

#### "확인 불가" (판단 보류, HTML 미반영)
- 없음. A-2의 3건은 모두 원본 파일 근거로 확정. 나머지는 정합 확인.
- 참고: 가이드의 바이옴 분류(초보용 단순화)는 원본의 세분 태그(`is_temperate`, `is_overworld` 등 광역)를 편집상 묶은 것으로, 오류가 아닌 의도된 단순화로 판단.

---

## 작업 B — 코드·기능 QA

실제 구동 QA는 Chrome 계열 바이너리/Playwright 부재로 **jsdom(무헤드 DOM, 스크립트 실제 실행)** 으로 페이지를 로드·구동해 수행.

### B-1. 검색창 XSS 점검 (필수) — **PASS (취약점 없음)**
- `search()` 함수(파일 내 검색 로직)는 사용자 입력 `v`/`t`를 **오직 `.filter()`/`.includes()` 매칭에만 사용**하고, `res.innerHTML`에 넣는 문자열은 **DEX/ITEMS 정적 데이터(k,e,i,c)와 고정 문자열("결과 없음"…)뿐**. 원문 쿼리는 결과 DOM에 **반영되지 않음**.
- 런타임 검증: 페이로드 `<img src=x onerror=window.__XSS=1>` 입력 → `window.__XSS === 0`(콜백 미실행), 결과 innerHTML에 `onerror` 문자열 미포함("결과 없음" 표시).
- ⇒ 미살균 반영 XSS **없음**. 수정 불필요.

### B-2. 기능·렌더·콘솔 점검
| 항목 | 결과 |
|------|------|
| jsdom 런타임 에러 | **0건** |
| 콘솔 error/warn | **0건** |
| 탭 수 / 전환 | 9개(진화·아이템 사전·화석·구조물·던전·바이옴 스폰·희귀·전설·상성표·미션·팁) 전부 클릭 에러 0 |
| 검색 동작 | 정상("피카"→피카츄 렌더) / clear 버튼 정상 |
| 상성표·미션·바이옴 렌더 | 정상, 예외 없음 |

**발견 버그 (최소 수정 반영):**

| # | 유형 | 내용 | 조치 | 근거 |
|---|------|------|------|------|
| B-2-1 | 데이터 중복 | DEX 배열에 `["킹드라","Kingdra"]` **2회 등록**(라인 713 정상 위치 + 라인 735 Gen2 배치에 중복). 검색 시 킹드라 중복 노출 유발. | 라인 735 중복 엔트리 **1건 삭제** (DEX 320→319) | 배열 파싱 시 동일 영문키 2회 검출 |

리팩토링/디자인 변경은 하지 않음(값·중복 데이터만 수정).

---

## 수정 요약 (HTML 반영 총 4건)

1. **A-2-1** 암멍이(Rockruff) 산·봉우리 행 삭제 — 1.7.3 스폰 정의 없음.
2. **A-2-2** 헬가(Houndoom) 네더 `보통(uncommon)` → `흔함(common)` — `0229_houndoom.json`.
3. **A-2-3** 앱솔(Absol) 타이가 `보통(uncommon)` → `희귀(rare)` — `0359_absol.json`.
4. **B-2-1** DEX 킹드라 중복 엔트리 1건 삭제.

수정 후 재검증: jsdom 에러 0 · 콘솔 0 · 탭/검색/XSS 정상 · 파일 86,351 bytes(<300KB) · 디자인/구조 불변.

## 미반영(정보성)
- 저장소 URL 이전(GitHub→GitLab): 문서 내 데이터 아님, 리포트에만 기록.
- 바이옴 단순화 분류: 의도된 편집으로 판단, 미수정.
